# Required only if you want real Claude generation.
# Without this key, the backend still runs and returns a demo/fallback deck.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional
CLAUDE_MODEL=claude-sonnet-4-20250514
